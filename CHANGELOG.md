# Changelog

本文件记录项目中的显著变更。
格式基于 Keep a Changelog，版本遵循 Semantic Versioning。

## [Unreleased]

## [V0.20] - 2026-05-26

### 新增
- 完成 PR-09 Slice 4 单日覆盖：7 天训练安排可进入训练日详情，支持今天和未来日期覆盖为其他模板、覆盖为休息日或重置为周期默认。
- 单日覆盖与首页今日计划卡共用有效计划推算，覆盖后 7 天视图、首页卡和从首页开始训练闭环会同步更新。
- 增加 Slice 4 核心测试与 UI 夹具，覆盖覆盖范围隔离、历史日期限制、已完成计划封锁、非计划训练不封锁、计划失效日期可覆盖和覆盖后开始训练闭环。

### 修复
- 修复单日覆盖被历史日期或已完成计划锁定时的错误文案，避免历史日期误提示为已完成计划。

## [V0.19] - 2026-05-26

### 新增
- 增加 PR-09 Slice 3 首页今日计划卡：按当前训练周期显示训练日、休息日、已完成、非计划训练和计划失效状态，并与「继续当前训练」卡互斥。
- 支持从今日计划一键开始训练，复用现有未结束训练冲突处理；完成后首页与未来 7 天训练安排同步更新。
- 增加 Slice 3 核心测试与 UI 夹具，覆盖今日计划卡状态、时区判断、上次同模板完成时间、从计划开始训练闭环和首页跳转 7 天视图。

## [V0.18] - 2026-05-26

### 新增
- 增加 PR-09 Slice 2 训练安排入口与未来 7 天视图：Settings 可进入训练安排页，支持无周期空态、今天高亮、模板颜色、休息日、计划失效和今日完成状态展示。
- 增加训练周期创建、编辑和删除 UI，支持起始日期、槽位模板/休息选择、增删与排序；删除周期会确认并清除单日覆盖记录。
- 增加 Slice 2 核心测试，覆盖 7 天展示、完成状态、模板删除/改名、颜色、时区、周期编辑保留覆盖、全休息拦截、周期删除和 Settings 入口导航。

### 修复
- 修复周期编辑页遇到模板已删除的失效槽位时无法保存的问题，保存时保留模板稳定身份并继续按「计划已失效」展示。

## [V0.17] - 2026-05-26

### 新增
- 增加 PR-09 Slice 1 推算引擎：支持训练周期创建校验、按周期时区推算日计划、单日覆盖与重置、完成状态计算和覆盖封锁判断。
- 增加 PR-09 Slice 1 核心测试，覆盖跨月/跨年槽位推算、覆盖优先与唯一性、5 种完成状态、模板删除后的稳定身份匹配、周期唯一性、session 写入路径和升级夹具推算。

## [V0.16] - 2026-05-26

### 新增
- 增加 PR-09 Slice 0 数据基础：训练周期、槽位、单日覆盖和模板稳定身份，为训练日历安排提供持久化数据契约。
- 训练 session 开始时记录模板稳定身份，并为旧模板和仍保留模板关系的旧 session 补齐身份，支持模板删除后的计划匹配语义。
- 增加模板颜色数据：预置 Push / Pull / Legs 模板写入默认颜色，用户新建模板保持无颜色。
- 增加 PR-09 Slice 0 核心测试，覆盖周期和覆盖持久化、模板删除后的计划失效、旧数据 backfill、覆盖唯一性、时区持久化和 seed 空库判断。

### 变更
- Seed Data 在非空库启动时补齐训练日历相关身份与颜色数据，并将训练周期、槽位和覆盖纳入空库判断，避免只剩训练安排数据时重新写入预置模板。

## [V0.15] - 2026-05-24

### 新增
- 模板编辑页「添加动作」支持底部多选面板，可从动作库选择多个动作并去重追加到模板动作列表，已在模板中的动作会显示为「已添加」并禁用重复选择。
- 添加动作面板支持内联新建动作，复用动作库名称校验，默认 90 秒休息且为双侧动作，创建后自动选中并可随本次选择追加到模板。
- 增加 `TemplateLibrary` 模板动作追加能力，统一处理追加去重、顺序持久化、legacy 关系同步，以及进行中训练使用该模板时的保护提示。
- 增加 PR-08 Slice 4 核心测试，覆盖动作追加、空模板起始顺序、本次选择去重，以及 open session 下阻止模板动作追加并保持原顺序。

## [V0.14] - 2026-05-24

### 新增
- 模板列表编辑态支持点进模板编辑页，可修改模板名称、查看模板内动作、删除动作并通过拖拽重排动作顺序。
- 模板编辑页增加「添加动作」占位入口，当前提示后续切片支持，避免提前引入 Slice 4 多选添加流程。
- 增加 `TemplateLibrary` 模板动作编辑能力，统一处理模板内动作删除、动作顺序持久化，以及进行中训练使用该模板时的保护提示。
- 增加 PR-08 Slice 3 核心测试，覆盖模板内动作删除、重排、连续顺序回写，以及 open session 下阻止模板动作变更。

## [V0.13] - 2026-05-24

### 新增
- Settings 增加「管理动作库」入口，支持查看动作库、创建/编辑动作、设置默认休息时间与单侧属性，并可通过滑动删除动作。
- 增加 `ExerciseLibrary` 服务，统一动作名 trim 与唯一性校验、默认休息时间下限、进行中训练保护，以及删除动作时清理模板引用并保留历史训练快照。
- 增加 PR-08 Slice 1 核心测试，覆盖动作库新建/编辑/删除校验、open session 保护、模板引用清理、历史快照保留和动作改名后的上次记录查询。
- 模板列表增加编辑态，支持进入/完成编辑、拖拽重排、删除模板、新建空模板，以及非编辑态长按重命名或删除模板。
- 增加 `TemplateLibrary` 服务，统一模板名 trim 与唯一性校验、新建模板追加排序、模板顺序持久化，以及删除模板时保留训练 session 与动作快照。
- 增加 PR-08 Slice 2 核心测试，覆盖模板新建、重命名、删除、重排、open session 删除模板后继续记录，以及历史模板名快照展示。

### 修复
- 修复同一动作改名后上次重量/次数参考可能断裂的问题，动作记录页改为优先按 `Exercise` 对象身份查询历史记录，并保留名称快照 fallback。
- 修复历史记录在模板改名后显示最新模板名的问题，改为优先使用训练完成时记录的模板名快照。
- 修复模板全部删除后无法重新进入编辑态新建模板的问题。

## [V0.12] - 2026-05-24

### 新增
- 增加 PR-08 Slice 0 数据基础：模板列表持久排序、模板内动作顺序关系和训练 session 动作快照，为后续动作库与模板 CRUD 提供稳定数据契约。
- 创建训练 session 时记录动作名称、顺序、默认休息时间和单双侧属性快照，支持模板后续删除或修改后继续当前训练。
- 增加 PR-08 Slice 0 核心测试，覆盖 seed 非恢复/非覆盖、模板顺序回填、动作顺序持久、session 快照、模板删除后继续记录和通知路由恢复。

### 变更
- Seed Data 改为仅空库首次写入预置动作与模板；非空库启动时只执行去重和回填，不再恢复已删除预置项，也不覆盖用户修改过的预置属性或模板动作顺序。
- 模板列表改为按 `Template.sortIndex` 展示，当前训练页、动作记录页和休息通知路由改为优先读取 session 动作快照。

### 修复
- 修复进行中训练依赖 live 模板关系的问题，避免模板删除或动作列表变化后当前训练动作列表、动作记录和通知跳转失效。

## [V0.11] - 2026-05-23

### 新增
- 记录页重量输入增加全局 `kg` / `lb` 单位偏好，默认 `kg`，输入框标签、上次参考组和当前训练已记录组会随所选单位显示。
- 训练历史详情页支持同一重量单位偏好，可按 `kg` 或 `lb` 查看历史组次重量。
- 增加重量单位换算、动作记录页输入草稿和历史详情重量展示测试，覆盖 `lb` 输入转 `kg`、预填换算、单位切换和展示格式。

### 变更
- `lb` 录入在提交前转换为 `kg` 写入 SwiftData，保持 `WorkoutSet.weight`、CSV 导出和衍生重量指标始终使用 `kg` 口径。

## [V0.10] - 2026-05-23

### 新增
- 增加 `ExerciseLoggingViewModel` 单元测试，覆盖动作记录页草稿输入解析、重量/次数步进、预填、侧别平衡和完成按钮文案。

### 变更
- 将 Swift 源码按 App、Models、DesignSystem、Services 和 Features 分层整理，拆出模板列表、当前训练、动作记录、训练历史和 Settings 等功能目录，便于后续按 Feature 独立开发。
- 将 `ContentView` 瘦身为根导航与 seed/通知路由壳，抽出 `AppRoute`、`TemplateListView` 和 `CurrentWorkoutView`，保持现有导航流程不变。
- 拆分休息计时与通知调度职责，将通知 payload/scheduler 移入 Services/Notifications，计时器 model/view/policy 留在 ExerciseLogging feature。
- 将训练历史的数据聚合与展示格式化逻辑拆入独立的 `WorkoutHistoryData`，保留现有历史列表、详情、分享和删除行为。
- 将动作记录页输入草稿状态和纯展示决策迁入 `ExerciseLoggingViewModel`，SwiftData 写入、删除和休息计时副作用仍保留在 View 层。

## [V0.9] - 2026-05-23

### 新增
- Settings 增加「训练历史」入口，支持按月分组查看非空训练 session，并进入只读详情查看本次训练的动作与组记录。
- 训练历史支持系统式多选，可选择多次训练生成单个合并 CSV 并通过 ShareLink 分享，导出仍保持原有 16 列 schema 与 UUID `session_id`。
- 训练历史支持批量删除已结束训练，并同步删除对应组记录；进行中的训练可查看与分享，但需先结束或丢弃后才能删除。
- 增加 App 图标资源与中文显示名称「大只仔」。

### 变更
- 统一部分按钮动效、阴影与间距 token，改善模板卡片、RPE 选择器和常用按钮的触感一致性。

### 修复
- 避免从历史批量删除进行中训练导致当前训练导航或休息通知状态悬空。

## [V0.8] - 2026-05-22

### 新增
- 增加 Archivo Black 与 JetBrains Mono 字体资源，并注册为 App 内可用字体。

### 变更
- 将模板卡片标题与数字排版切换为自定义品牌字体，使训练模板与重量、次数、倒计时等数值信息更有辨识度。
- 将 RPE 选择器的选中态改为按强度分级配色，区分低强度、中强度、接近力竭和高风险强度。

## [V0.7] - 2026-05-21

### 新增
- 增加 DaaiZekBro 视觉设计系统，统一颜色、间距、卡片、按钮、分割线、数字排版、模板卡片、RPE 选择器和已记录组徽标等组件。
- 增加品牌 logo 图片资源，并在模板列表导航栏展示；同步更新 App accent color。

### 变更
- 将模板列表、当前训练页、动作记录页和 Settings 改为统一的卡片化视觉风格，提高训练现场快速扫读与操作的清晰度。
- 重新设计重量/次数输入、RPE 展开选择、已记录组列表、休息倒计时和 CSV 导出 sheet 的界面样式，使主要操作与数值信息更突出。

## [V0.6] - 2026-05-21

### 新增
- Settings 导出入口增加时间范围选择 sheet，支持「全部」「最近 7 天」「最近 30 天」和自定义起止日期导出 CSV。
- CSV 导出支持按设备当前时区的本地日、基于 `WorkoutSet.completedAt` 裁剪训练组，跨午夜训练会按组完成时间落入对应日期范围。
- 范围选择页显示实时预览「将导出 N 个 session · M 个组」，空范围或自定义日期倒置时禁用导出并展示对应提示。
- 范围导出使用临时文件与 `ShareLink` 分享，并按范围生成 `gym_log_full_YYYY-MM-DD.csv`、单日或多日区间文件名。
- 增加 CSV 范围导出测试，覆盖全量导出一致性、最近 7/30 天边界、自定义单日、空范围、跨午夜、未结束 session、文件名和预览计数一致性。

## [V0.5] - 2026-05-21

### 修复
- 结束、丢弃或替换当前训练时清理休息倒计时通知，避免 session 已关闭后仍收到旧提醒。
- 单侧动作在倒计时期间删除右侧记录后，会重新根据左右记录数推导下一侧，并阻止左右状态不匹配时误保存。
- 通知点击携带的动作不属于当前训练模板时，回退到当前训练页，避免进入不匹配的动作记录页。

### 新增
- 增加 PR-07 Core MVP 边界测试，覆盖单侧动作删除后侧别推导、已结束 session 禁止写入、非法输入不落库、side 约束、快照预填 fallback 和模板外通知导航。

## [V0.4] - 2026-05-21

### 新增
- 在训练记录页增加组间倒计时，完成双侧动作或单侧动作右侧后自动进入计时态，显示圆环倒计时与剩余时间。
- 支持倒计时 `+30 秒` 延长和「跳过」操作，跳过后立即回到记录态。
- 集成本地通知权限请求、休息结束通知调度、通知替换与取消；用户未开启通知时，前台倒计时仍可用并提示「未开启通知，仅前台显示」。
- 增加通知点击导航：训练仍未结束时返回对应训练 session 的动作记录页，训练已结束或不存在时返回模板列表。
- 增加倒计时与通知导航测试，覆盖计时状态、通知替换、跳过、离开页面保留通知、单侧触发条件和通知点击导航分支。

### 修复
- 避免倒计时通知调度被替换或跳过后，旧的异步调度任务继续写入过期通知。

## [V0.3] - 2026-05-21

### 新增
- 增加 set-level CSV 导出器，支持全量导出非空训练 session、固定 16 列字段、UTF-8 BOM、ISO 8601 时区时间戳、CSV 字段转义和 `gym_log_YYYY-MM-DD.csv` 文件名。
- 导出内容支持空 RPE、空 side、未结束 session 的空结束时间、单侧动作 `left` / `right` 标记，以及 `volume_kg_rep`、`e1rm_epley_kg` 衍生列。
- 增加 Settings 屏与模板列表右上角设置入口，支持生成 CSV 后通过系统 ShareLink 分享，并展示负重口径与单侧动作记录说明。
- 增加 CSV 导出测试，覆盖字段顺序、数据正确性、空 session 跳过、CSV 转义、BOM、文件名、单侧动作边界和无效时区错误。

## [V0.2] - 2026-05-21

### 新增
- 将训练记录页从占位实现替换为可用记录流程，支持重量、次数、RPE 整数选择和上次值预填。
- 支持双侧与单侧动作的记录流程，单侧动作可按左右侧独立记录、恢复未完成半组，并展示对应侧的上次记录。
- 支持当前 session 内已记录组展示、删除确认和按完成时间自动重编号。
- 增加 WorkoutSet 记录规则测试，覆盖单侧半组恢复、setIndex 连续性、exerciseOrderIndex、删除重编号、RPE 约束和跨 session 隔离。

## [V0.1] - 2026-05-21

### 新增
- 定义训练记录 MVP 的 SwiftData 数据模型，包括动作、模板、训练 session、训练组记录与单侧动作枚举。
- 添加 19 个预置训练动作与 6 个训练模板的 Seed Data，并支持按名称去重写入与模板动作关系修复。
- 增加模板列表入口，可按固定顺序展示 6 个训练模板并从模板开始训练。
- 增加当前训练页，展示训练时长、模板动作列表、默认休息时间和当前 session 内已记录组数。
- 增加 WorkoutSession 生命周期管理，支持创建、结束、丢弃训练，并处理未结束训练冲突。
- 增加「继续当前训练」入口，支持通过 `templateNameSnapshot` 在模板关系缺失时恢复动作列表。
- 增加占位训练记录页，为后续动作组记录流程提供导航入口。
- 补充 in-memory SwiftData 测试，覆盖 Seed Data、Session 生命周期、模板 fallback、组数统计和未结束训练约束。

### 变更
- 将默认 Xcode 模板中的 `Item` 示例模型替换为训练记录 App 的实际 SwiftData schema。
- 将临时 PR-01 Seed Data 验收界面替换为训练记录 MVP 的模板列表与当前训练导航流程。
